[phases.setup]
nixPkgs = ["python311", "unixODBC-devel", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:server --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
